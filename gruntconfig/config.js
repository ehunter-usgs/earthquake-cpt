'use strict';

var config = {
  src: 'src',
  build: '.build',
  buildPort: 8050,
  dataPort: 8055,
  dist: 'dist',
  distPort: 8052,
  liveReloadPort: 8059,
  templatePort: 8054
};

module.exports = config;
